export type Metadata = {
  title: string;
  description: string;
  date: string;
  readingTime: number;
  tags: string[];
};

export type Post = {
  title: string;
  slug: string;
  description: string;
  date: string;
  readingTime: number;
  tags: string[];
};
